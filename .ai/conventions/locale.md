# Locale (MVP)

Single language: **id**. Fallback: **id**. No bilingual product (DEC-025).

Implementation must update `.env` and `config/app.php` (still `en` in the starter). Framework validation/auth/datetime strings must use Indonesian localization.
