# Adopted baseline metadata

In `PROJECT.md`, **Baseline version** (`1.0.0`) and **Adopted on** (`2026-09-01`, ISO 8601) are **immutable**.

Do not change, “refresh”, or bump these fields during ordinary documentation harvest, stack updates, or adaptation follow-ups.

Change them only when the Oracle gives an **explicit baseline-upgrade** instruction.
